<%= render "teachers/teachers_list",
    {
      teacher_list_var: @teachers,
      name_method: :last_first,
      compact_var: "compact",
      allow_add_var: (admin_logged_in?  ? "allow" : "")
    } %>
